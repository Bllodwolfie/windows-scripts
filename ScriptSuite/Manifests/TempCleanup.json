{
  "id": "TempCleanup",
  "displayName": "Temp File Cleanup",
  "description": "Deletes files from the Windows temp folder that are older than the cutoff.",
  "category": "Cleanup",
  "requiresAdmin": false,
  "supportsDryRun": true,
  "scriptPath": "scripts/TempCleanup/TempCleanup.ps1",
  "fields": [
    {
      "name": "TargetFolder",
      "label": "Folder to clean",
      "type": "path",
      "default": "%TEMP%",
      "helpText": "The temp folder this script cleans. Files older than the cutoff will be deleted from here.",
      "helpDetail": "This is usually %TEMP% — Windows' temporary files. Files go to the Recycle Bin, so you can restore them. You can point this at another folder, but keep it to a temp-like location."
    },
    {
      "name": "CutoffDays",
      "label": "Delete files older than",
      "type": "int",
      "unit": "days",
      "default": 7,
      "helpText": "Files in the temp folder older than this many days will be deleted.",
      "helpDetail": "Based on last write time. Younger files are left alone. 7 days keeps recent temp files for apps that are still running."
    }
  ]
}