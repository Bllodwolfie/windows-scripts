<Window x:Class="ScriptSuite.ResumePromptWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Script Suite — Interrupted Run" Width="560" SizeToContent="Height"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize" ShowInTaskbar="False"
        Background="#FF1E1E2E" Foreground="#FFCDD6F4">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Padding" Value="16,6" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Background" Value="#FF313244" />
            <Setter Property="Foreground" Value="#FFCDD6F4" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd" Background="{TemplateBinding Background}"
                                CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="#FF45475A" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <StackPanel Margin="24,20,24,20">
        <StackPanel Orientation="Horizontal">
            <TextBlock Text="&#x26A0;" FontSize="20" Foreground="#FFF9C74F" VerticalAlignment="Top" />
            <TextBlock x:Name="TitleText" FontSize="17" FontWeight="SemiBold" Foreground="#FFCDD6F4"
                       Margin="10,2,0,0" TextWrapping="Wrap" />
        </StackPanel>
        <TextBlock x:Name="MessageText" FontSize="13" Foreground="#FFBAC2DE" TextWrapping="Wrap" Margin="30,12,0,0" />
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,22,0,0">
            <Button x:Name="DiscardButton" Content="Discard" Click="DiscardButton_Click"
                    Margin="0,0,8,0" AutomationProperties.Name="Discard" />
            <Button x:Name="ResumeButton" Content="Resume" Click="ResumeButton_Click"
                    FontWeight="SemiBold" IsDefault="True" AutomationProperties.Name="Resume" />
        </StackPanel>
    </StackPanel>
</Window>