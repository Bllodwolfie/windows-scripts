<Window x:Class="ScriptSuite.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="clr-namespace:ScriptSuite.Controls"
        mc:Ignorable="d"
        Title="Settings" Height="640" Width="720"
        MinHeight="400" MinWidth="560"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource Brush.Background.Primary}" Foreground="{DynamicResource Brush.Foreground.Primary}">
    <Grid Margin="24,18,24,20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock x:Name="ScriptNameText" Grid.Column="0" FontSize="22" FontWeight="Bold" Foreground="{DynamicResource Brush.Foreground.Primary}" VerticalAlignment="Center" />
            <Button x:Name="HelpButton" Grid.Column="1" Content="?" Width="22" Height="22" Margin="8,0,0,0" FontSize="11" FontWeight="SemiBold"
                    Foreground="{DynamicResource Brush.Accent.Primary}" Background="Transparent" BorderBrush="{DynamicResource Brush.Accent.Primary}" BorderThickness="1"
                    Click="HelpButton_Click" Visibility="Collapsed" AutomationProperties.Name="Help">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="11" />
                    </Style>
                </Button.Resources>
            </Button>
        </Grid>
        <TextBlock x:Name="ScriptDescriptionText" Grid.Row="1" Foreground="{DynamicResource Brush.Foreground.Secondary}" TextWrapping="Wrap"
                   Margin="0,4,0,0" />

        <Border x:Name="HelpPanel" Grid.Row="2" Background="{DynamicResource Brush.Background.Primary}" CornerRadius="6" Padding="10" Margin="0,8,0,0" Visibility="Collapsed">
            <TextBlock x:Name="HelpTextBlock" Foreground="{DynamicResource Brush.Foreground.Secondary}" FontSize="12" TextWrapping="Wrap" />
        </Border>

        <TextBlock x:Name="NoFieldsText" Grid.Row="3" Foreground="{DynamicResource Brush.Foreground.Help}" FontSize="13"
                   TextWrapping="Wrap" Margin="0,16,0,0"
                   Text="This script has no configurable settings."
                   Visibility="Collapsed" />

        <controls:SettingsForm x:Name="SettingsFormControl" Grid.Row="4" Margin="0,12,0,0" />

        <!-- Advanced per-extension rules — DownloadsCleanup only (additive, collapsed by default).
             Visible only for DownloadsCleanup; other scripts keep original SettingsForm only. -->
        <controls:DownloadsAdvancedRulesControl x:Name="AdvancedRulesControl" Grid.Row="5" Margin="0,8,0,0"
                                               Visibility="Collapsed" />
    </Grid>
</Window>

